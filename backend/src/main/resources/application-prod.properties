spring.datasource.url=${DB_URL:jdbc:postgresql://147.79.75.208:5432/sgdis_db}
spring.datasource.username=${DB_USERNAME:sgdis_user}
spring.datasource.password=${DB_PASSWORD:VendeHumos2025}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret.password=${JWT_SECRET_KEY:mySecretKey123456789012345678901234567890}
jwt.issuer.generator=sgdis-backend

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:sgdis.sena@gmail.com}
spring.mail.password=${MAIL_PASSWORD:jkze swpi hcsl jynw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Base URL for password reset links
app.base-url=${APP_BASE_URL:http://sgdis.cloud}

# Multipart file upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Timezone configuration - Colombia (GMT-5)
spring.jackson.time-zone=America/Bogota